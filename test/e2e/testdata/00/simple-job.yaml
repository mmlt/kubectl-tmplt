prune:
  labels:
    gitops.example.com/repo: testdata-00
  namespaces:
    - default

steps:
  - tmplt: "template.yaml"
    values:
      name: "test"
  - wait: --for condition=Ready pod -l app=example

defaults:
  namespace: "default"